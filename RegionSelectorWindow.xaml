<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="VedionScreenShare.RegionSelectorWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    WindowStyle="None"
    AllowsTransparency="True"
    Background="Transparent"
    Topmost="True"
    Left="0" Top="0"
    ShowInTaskbar="False"
    Title="Select Region">

    <!-- Full-screen dark overlay — Background must be set for mouse events to fire -->
    <Canvas x:Name="MainCanvas" Background="#88000000"
            MouseLeftButtonDown="Canvas_MouseDown"
            MouseMove="Canvas_MouseMove"
            MouseLeftButtonUp="Canvas_MouseUp">

        <!-- Instruction -->
        <Border x:Name="HintBorder" Canvas.Left="0" Canvas.Top="0">
            <TextBlock x:Name="HintText"
                       Margin="20,16"
                       Text="Click and drag to select region. Press Escape to cancel."
                       Foreground="White" FontSize="15" FontWeight="Bold"/>
        </Border>

        <!-- Selection box -->
        <Rectangle x:Name="SelectionRect"
                   Stroke="#00BFFF" StrokeThickness="2"
                   Fill="#3300BFFF"
                   Visibility="Collapsed"/>

        <!-- Dimensions label -->
        <TextBlock x:Name="SizeLabel"
                   Foreground="White" FontSize="12" FontWeight="Bold"
                   Background="#AA000000" Padding="4,2"
                   Visibility="Collapsed"/>
    </Canvas>
</Window>
